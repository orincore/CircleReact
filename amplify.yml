version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npx expo install --fix
    build:
      commands:
        - npx expo export --platform web
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .expo/**/*
