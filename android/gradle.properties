# CRITICAL: AndroidX Configuration - MUST be at the very top
# These properties are required for all AndroidX dependencies to work
android.useAndroidX=true
android.enableJetifier=true

# Suppress SDK version warnings
android.suppressUnsupportedCompileSdk=36

# R class optimizations
android.nonTransitiveRClass=true
android.nonFinalResIds=true

# Project-wide Gradle settings.

# Specifies the JVM arguments used for the daemon process.
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
newArchEnabled=true

# Use this property to enable or disable the Hermes JS engine.
hermesEnabled=true

# Use this property to enable edge-to-edge display support.
edgeToEdgeEnabled=true

# Enable GIF support in React Native images
expo.gif.enabled=true
# Enable webp support in React Native images
expo.webp.enabled=true
# Enable animated webp support
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# Edge-to-edge config
expo.edgeToEdgeEnabled=true

# SDK versions
android.minSdkVersion=24
android.compileSdkVersion=35
android.targetSdkVersion=35
android.ndkVersion=27.0.12077973

# Release Keystore Configuration
CIRCLE_UPLOAD_STORE_FILE=circle-release-key.keystore
CIRCLE_UPLOAD_KEY_ALIAS=acb48b393cf48068a3cb4081f571cdb5
CIRCLE_UPLOAD_STORE_PASSWORD=1b244969565d4c2e3f7765e8e3839b04
CIRCLE_UPLOAD_KEY_PASSWORD=b242f35bbd34af9983d87455957614a0
